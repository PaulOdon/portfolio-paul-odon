/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
      "www.soka.club",
      "object-if.online",
      "etechconsulting-mg.com",
      "images.unsplash.com",
      "plus.unsplash.com",
      "postimg.cc",
    ],
  },
};

export default nextConfig;
